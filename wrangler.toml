# Cloudflare Pages – wrangler.toml
# Dùng để deploy bằng Wrangler CLI (tùy chọn)
# Nếu dùng GitHub CI/CD thì không cần file này.

name = "hatinh-vip"
compatibility_date = "2024-01-01"

# Pages project sử dụng thư mục gốc làm static assets
pages_build_output_dir = "."

# Biến môi trường sẽ được set trên Cloudflare Dashboard
# KHÔNG lưu key thực vào đây!
# [vars]
# SUPABASE_URL = "https://your-project.supabase.co"
# SUPABASE_ANON_KEY = "your-anon-key"
